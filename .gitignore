# Secrets & local config
.env
config.yaml

# State
state.db
*.db-journal

# Generated dashboard (regenerated hourly from state.db; published to gh-pages)
docs/

# Dashboard publish (Phase 2): PAT, gh-pages worktree, lock — never commit these
.gh-token
.gh-pages-wt/
.publish.lock

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/

# launchd/cron logs
*.log

# playwright research artifacts
.playwright-mcp/
